{
  "name": "react_2",
  "version": "1.0.0",
  "browserify": {
    "transform": [
      "reactify"
    ]
  },
  "description": "testing out how the hell to set up a js toolchain",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max",
  "license": "MIT",
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "scripts": {
  "test": "jest"
  },
  "devDependencies": {
    "jest-cli": "^0.9.0"
  }
}
